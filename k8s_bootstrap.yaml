# Playbook to bootstrap k8s cluster
# ansible-playbook --user debian --inventory k8s_hosts.ini --extra-vars ansible_python_interpreter=python3 ./k8s_boostrap.yaml
---
- hosts:
    - k8s
  roles:
    - geerlingguy.kubernetes
  pre_tasks:
    # TODO fixup apt preference for default release
    - name: Update apt cache
      command: apt update
